<!DOCTYPE html>
<html>
  <head>
    <title>RodauthRailsTailwindDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <main class="mx-auto container px-4 sm:px-6 lg:px-8 h-screen">
      <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" data-controller="dark-mode" data-action="dark-mode#toggle" >
        Toggle Dark Mode
      </button>
      <%= render "layouts/nav_bar"  %>
      <%= yield %>
    </main>
  </body>
</html>